# AI-Powered Network Traffic Analyzer — Requirements
#
# Install with:
#   pip install -r requirements.txt
#
# Npcap (Windows packet capture driver) must also be installed separately:
#   https://npcap.com/   (free, choose "WinPcap API-compatible mode" during install)

# ── Core ML ───────────────────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
tensorflow>=2.13          # Keras bundled — ae_model.keras format
xgboost>=1.7              # XGBoost classifier (Phase 1 addition)

# ── Model persistence ─────────────────────────────────────────────────────────
joblib>=1.3

# ── Dashboard ─────────────────────────────────────────────────────────────────
dash>=2.14
plotly>=5.17

# ── Packet capture ────────────────────────────────────────────────────────────
scapy>=2.5                # requires Npcap on Windows
